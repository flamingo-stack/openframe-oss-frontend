# Build output and caches
dist
.next
node_modules
.yalc
*.tsbuildinfo
coverage

# Code generators own these — reformatting them produces a diff on every
# regeneration, in a file nobody edits by hand.
src/__generated__
src/generated

# Tool-owned.
package-lock.json
schema.graphql

# Not formatted by anything today; including them would bury the code diff.
*.md
*.mdx
