<!-- source-hash: 602a28f048fb0002ba6aacec5c77d128 -->
Barrel export file for the `DeviceSelector` component, re-exporting the component and its associated TypeScript types from their respective modules.

## Key Components

- **`DeviceSelector`** — The main device selector component
- **`DeviceSelectorProps`** — TypeScript props interface for the `DeviceSelector` component
- **`InfiniteScrollConfig`** — TypeScript interface for configuring infinite scroll behavior

## Usage Example

```typescript
import { DeviceSelector } from '@/components/device-selector';
import type { DeviceSelectorProps, InfiniteScrollConfig } from '@/components/device-selector';
```

## Source

[`index.ts`](https://github.com/flamingo-stack/openframe-oss-frontend/blob/main/src/components/device-selector/index.ts)