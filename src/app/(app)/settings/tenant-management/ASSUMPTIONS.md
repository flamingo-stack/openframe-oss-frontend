# Tenant Management — assumptions and open questions (CU-86akj8ajt)

UI-only module built on a mock service while the directory backend
(`openframe-saas-tenant` branch `feature/directory-fe-integration`) is unmerged.
Every open product question below was answered with an assumption that has ONE
edit point in the code; when the real answer lands, change that point, update
this file and commit the change on its own. Delete rows as they close; delete
the file when the last one does.

| # | Assumption | Edit point | Owner of the answer |
|---|---|---|---|
| A1 | Microsoft 365 shows the consent-link flow the Figma frames draw. The FE facade this module is built against (`feature/directory-fe-integration`, 2026-09-15) still sits on the old provider API — customer-owned Entra app registration (`createMicrosoft365Config`: msTenantId / clientId / clientSecret), so its `consentUrl` is null for Microsoft. The stacked backend chain that is actually being merged (`split/08-ms365-api`, commit `d55e96de1` of 2026-09-17) replaced that with admin consent: `createMicrosoft365Config(name, msTenantId)` → PENDING, `startMicrosoft365Consent(tiers: CORE/AUDIT/WRITE/ROLES) → authorizationUrl`, `testMicrosoft365Connection` = CORE-tier probe, `msTenantId` frozen once CORE is ACTIVE. That is the Figma flow; what remains open is whether `msTenantId` accepts the verified domain the form collects (the adminconsent URL does) or needs the directory GUID, and when the facade is rebased onto the chain. | `components/consent/microsoft-consent-panel.tsx` | Andrii Yakovenko / Andrii Kachur; product: Ilona |
| A2 | Non-owners see the list and the details page read-only (no Connect / Reconnect / Edit); `/new`, `/edit`, `/reconnect` render the shared `LockedScreen` ("Owners only"), not a 404 — the billing precedent. | `new/page.tsx`, `edit/page.tsx`, `reconnect/page.tsx`; `actions` in `tenant-list-view.tsx` / `tenant-details-view.tsx` | Ilona |
| A3 | Visibility: feature flag `tenant-management` (dev bypass in `use-tenant-management-gate.ts`), dark on qa/prod until the backend registers the name; hub card carries a `warning` "Beta" tag; shown in both OSS and SaaS modes. | `hooks/use-tenant-management-gate.ts`, `settings-hub.tsx` | Ilona + DevOps |
| A4 | "Last read" = `lastSyncAt` (the directory read), not `access.checkedAt` (the probe); rendered with the app's `formatRelativeTime` ("41m ago") and the calendar date once older than a day. `lastSyncStatus` / `lastSyncError` are not shown. | `utils/tenant-presentation.ts` → `lastReadAt()`, `formatLastRead()` | Ilona / Andrii / Roman |
| A5 | The record is created on the first "Generate Connection Link" (`createDirectoryConnection` returns it with the link), so leaving the New page after that leaves a `DISCONNECTED` row behind. "Generate" is disabled until the form is complete (frames 2097-122192 vs 2097-122207); "Edit Domain" re-runs as `updateDirectoryConnection(domain, …)` + `startDirectoryConsent`, and disappears once a probe succeeded (the backend refuses to move a consented domain). "Save Integration" = `updateDirectoryConnection(name, organizationId)` only when they changed since, then `router.replace(details)`; enabled from the moment a link exists, disabled while a check runs. Reconnect mints a fresh link on entry and its "Save Integration" only returns to the details page (nothing else to write). | `hooks/use-new-tenant-flow.ts`, `components/reconnect-tenant-view.tsx` | Ilona |
| A6 | Copy is verbatim from Figma ("Back to Integrations", "New Tenant Integration") except the double space in "Check  Connection". "Back to Integrations" on Edit / Reconnect is the app's safe-back (history when there is one, the list otherwise), so from the details page it returns there; the list page keeps the "Back" every settings sub-page has, which the list frame omits. Dates use the app's shared formatters (`2/15/26 9:03:19 PM`, "41m ago") where the frames draw `11/12/24 09:43:00` and "41 minutes ago". | `components/consent/consent-block.tsx`, views | Ilona / Roman |
| A7 | `NOT_AUTHORISED` (in the schema, not in Figma) renders as an `error` tag "Not authorised". Failed check, empty list, loading and error states use the repo's shared vocabulary (`EmptyState`, `SectionLoadError`, page skeletons). | `utils/tenant-presentation.ts`, views | Roman |
| A8 | Field order is Domain Name → Connection Name → Select Customer (four of the five New frames); Microsoft is pre-selected; the Google radio description is kept as drawn. Google consent copy mirrors Microsoft's with Super Admin wording (no Google frame exists). | `types/tenant-form.types.ts` defaults, `utils/tenant-presentation.ts` `PROVIDER_PRESENTATION` | Roman / Ilona |
| A9 | No mobile/tablet frames: below `md` the layout collapses (one-column forms, hidden Customers/open columns, Save in the mobile action bar); at `md` (800px) the form grid is two columns (domain on its own row) because a four-way split leaves 156px per field — four columns from `lg`. No pixel parity below 1280. A refresh in the middle of the New flow resets it (state is in memory). | views, `tenant-form-fields.tsx` `FIELD_GRID` | Roman |
| A10 | A connection always has exactly one customer (no "standalone MSP tenants"); customers already bound to a connection are hidden from the picker (Edit asks for its own back); the picker exhausts pages up to 500 and has no search (Ilona, Figma #107); with no customer left to bind it is a disabled field reading "No customers available". | `types/tenant-form.types.ts` `tenantCustomerFieldSchema`, `hooks/use-tenant-connections.ts` `useTenantOrganizations`, `components/customer-select.tsx` | Ilona / Michael |
| A11 | The list/table accept `organizationId` + `embedded` so a Customer → Integrations tab can host them later; the tab itself is not built. | `components/tenant-list-view.tsx` | Ilona |

## Data gaps (no assumption possible — waiting)

- When the provider-agnostic FE facade (`feature/directory-fe-integration`: `directoryConnections(filter)`, `createDirectoryConnection`, `startDirectoryConsent`, `checkDirectoryConnection`, `DirectoryAccessState`) is re-carved onto the `split/*` chain and reaches a PR environment, so `schema.graphql` can be pulled (`npm run fetch-schema`). The chain itself (`split/07-directory-core` … `split/11-ai-agent-tools`, 2026-09-17) exposes only per-provider `create*/update*/start*Consent/test*Connection` plus tenant-wide `directoryConnections` and `attach/detachDirectoryConnection` (many-to-many, phase 2). `test-2251.dev3.openframe.build` is a live per-PR environment (backend PR 2251, Google directory tools QA of 2026-09-11) — its schema predates both. — Andrii Yakovenko / Andrii Kachur
- QA levers in the mock (no UI): domains ending in `.notauthorised.test`, `.revoked.test`, `.disconnected.test`, `.writeavailable.test`, `.writeenabled.test` fix the probe's answer; `.error.test` makes it fail; `.slow.test` keeps it loading. The seeded Stonebridge / Brightline rows refuse the FIRST probe and recover on the next one.
- Whether `msTenantId` on Microsoft create may be the customer's verified domain (what the New form collects, and what the adminconsent URL accepts) or must be the directory GUID. — Andrii Kachur
- The consent instructions for Google Workspace and for the "two-sided MSP" case (Ilona, 04.09: "waiting for the instructions"). — Ilona / Ivan

## Replacing the mock

1. `npm run fetch-schema -- --endpoint <feature tenant> --token <JWT>` → `npm run generate-enums`.
2. `types/directory-enums.ts` → re-export from `@/generated/schema-enums` (the `satisfies Record` tables then flag any new value).
3. Implement `ITenantConnectionsService` over Relay (or move the hooks to Relay fragments) in `services/tenant-connections-service.ts`; delete `tenant-connections-fixtures.ts` and the mock class.
